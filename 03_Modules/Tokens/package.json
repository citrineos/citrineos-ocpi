{
  "name": "@citrineos/ocpi-tokens",
  "version": "0.1.1",
  "description": "The Tokens module for OCPI v2.2.1. This module is not intended to be used directly, but rather as a dependency for other modules.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepublish": "npx eslint",
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npm run clean && tsc --build --verbose",
    "clean": "rm -rf dist/* tsconfig.tsbuildinfo",
    "fresh": "rm -rf node_modules package-lock.json && npm run clean"
  },
  "keywords": [
    "ocpi",
    "ocpp",
    "ocpp_v201"
  ],
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/deasync-promise": "^1.0.0",
    "eslint": "^8.48.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@citrineos/ocpi-base": "0.1.1",
    "@citrineos/base": "1.3.1",
    "@citrineos/data": "1.3.1",
    "@citrineos/util": "1.3.1",
    "deasync-promise": "^1.0.1",
    "tslog": "^4.9.2"
  }
}
