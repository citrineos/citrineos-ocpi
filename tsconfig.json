{
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "rootDir": "./",
    "outDir": "./dist"
  },
  "exclude": [
    "./dist/**/*",
    "**/node_modules/**",
    "DirectusExtensions",
    // todo for now
    "Swarm",
    // todo for now,
    "Server_OLD"
  ],
  "references": [
    {
      "path": "Server"
    }
  ]
}
